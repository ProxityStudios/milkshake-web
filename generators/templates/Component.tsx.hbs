import { Box, BoxProps } from '@chakra-ui/react';

type {{pascalCase name}}ComponentProps = BoxProps & {};

const {{pascalCase name}}Component: React.FC<{{pascalCase name}}ComponentProps> = (props) => {
    return (
      <Box {...props}>{{pascalCase name}}</Box>
    )
};

export default {{pascalCase name}}Component;
